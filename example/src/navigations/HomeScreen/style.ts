import {StyleSheet} from 'react-native';
import {XL} from '@xrnjs/ui';

export default StyleSheet.create({
  titleWrapper: {
    paddingHorizontal: XL,
    paddingTop: XL,
  },

  titleStyle: {
    color: '#8c8b8c',
  },
});
