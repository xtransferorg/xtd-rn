import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  wrap: {
    backgroundColor: '#fff',
    padding: 16,
    // minHeight: 100,
  },
  head: {
    paddingVertical: 16,
    fontWeight: '600',
    fontSize: 16,
  },
  customerContent: {
    marginTop: 16,
  },
  margBtm: {
    marginBottom: 8,
  },
});
