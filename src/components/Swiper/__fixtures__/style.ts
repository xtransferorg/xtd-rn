import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  wrapper: {
    padding: 24,
    borderWidth: 1,
    borderColor: 'rgba(0, 0, 0, 0.10)',
  },

  height: {
    height: 260,
  },
});
