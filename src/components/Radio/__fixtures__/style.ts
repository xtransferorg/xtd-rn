import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  section: {
    paddingHorizontal: 10,
    marginTop: 10,
    backgroundColor: '#fff',
    paddingBottom: 12,
    borderRadius: 4,
  },
  title: {
    fontWeight: '500',
    fontSize: 16,
    marginTop: 10,
    marginBottom: 12,
  },
});
