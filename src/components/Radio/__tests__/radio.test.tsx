// import React from 'react';
// import { render, fireEvent } from '@testing-library/react-native';
// import Radio from '../Radio';

describe('Radio Component', () => {
  test('test', () => {
    expect(1).toBe(1);
  });
  // it('initializes with the correct value', () => {
  //   const { getByRole } = render(
  //     <Radio options={[{ label: '', value: 'testValue' }]} value="testValue" />
  //   );
  //   expect(getByRole('radiogroup')).toBe('testValue');
  // });

  // it('calls onChange with the correct parameters when value changes', () => {
  //   const mockOnChange = jest.fn();
  //   const { getByLabelText } = render(
  //     <Radio
  //       onChange={mockOnChange}
  //       options={[
  //         { value: '1', label: 'Option 1' },
  //         { value: '2', label: 'Option 2' },
  //       ]}
  //     />
  //   );

  //   fireEvent(getByLabelText('Option 1'), 'onPress');
  //   expect(mockOnChange).toHaveBeenCalledWith('1', expect.anything());
  // });
});
