import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  wrapper: {
    paddingBottom: 30,
  },
  section: {
    marginTop: 10,
    backgroundColor: '#fff',
    paddingBottom: 14,
  },
  amount: {
    // fontSize: 22,
    // fontWeight: '600',
    // textAlignVertical: 'center',
    // borderBottomWidth: 0,
  },
});
