import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  wrapper: { maxWidth: 380 },
  container: {
    paddingBottom: 20,
  },
  section: {
    marginTop: 10,
    backgroundColor: '#fff',
    paddingVertical: 12,
    borderRadius: 4,
  },
  title: {
    paddingHorizontal: 10,
    fontWeight: '500',
    fontSize: 16,
    marginBottom: 12,
  },
});
