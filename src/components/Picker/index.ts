import { attachPropertiesToComponent } from '../../core/helpers';
import { Instance, Component, PickerComponent } from './PickerInstance';

export default attachPropertiesToComponent(Instance, {
  Component,
  PickerComponent,
});
