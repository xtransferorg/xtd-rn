import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  tabChildrenWrapper: {
    flexDirection: 'row',
  },
  scrollVertical: {
    paddingBottom: 60, //默认tabbar的头部高度44 + 预留16
    paddingHorizontal: 12,
  },
});
